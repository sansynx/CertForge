services:
  - type: web
    name: certforge
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -c gunicorn.conf.py app:app
    envVars:
      - key: FLASK_DEBUG
        value: "0"
    autoDeploy: true
